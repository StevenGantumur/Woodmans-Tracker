{
  "name": "woodmans-tracker",
  "private": true,
  "version": "1.0.0",
  "description": "Root package for Woodmans Tracker workspaces",
  "scripts": {
    "dev": "npm run dev --workspace backend & npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "dev:frontend": "npm run dev --workspace frontend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "dependencies": {
    "dotenv": "^17.2.3",
    "pg": "^8.17.2"
  }
}
